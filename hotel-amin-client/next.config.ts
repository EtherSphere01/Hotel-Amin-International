import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['longbeachhotelbd.com', 'www.solimarinternational.com', 'images.unsplash.com'],
  },
};

export default nextConfig;
